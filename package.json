{
  "name": "oura_raw_data",
  "version": "1.0.0",
  "description": "Fetch and export Oura API data through a local web UI.",
  "private": true,
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node server.js"
  },
  "dependencies": {
    "axios": "^1.9.0",
    "express": "^5.1.0"
  },
  "engines": {
    "node": ">=18"
  }
}
